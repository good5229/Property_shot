# Security Rules

- Never commit secrets, credentials, tokens, runtime config, private data, or
  production data.
- Treat destructive operations as explicit decisions.
- Keep logs and docs free of sensitive values.
- Prefer least privilege in tooling and access.
