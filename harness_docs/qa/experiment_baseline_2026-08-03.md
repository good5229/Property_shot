# 최종 품질 실험 기준선

기준 시각: 2026-08-03 KST  
브랜치: `commercial/wall-physics-qa`  
코드 기준 커밋: `7b775ab`
원격: `origin/commercial/wall-physics-qa`에 코드 커밋 Push 완료  
작업 트리: QA 산출물·시각 캡처 커밋 전

## 현재 자동 검증 기준

| 항목 | 결과 |
|---|---|
| 전체 `flutter test` | 233개 통과 |
| 다중샷 물리 분석 | 1개 통과 |
| 저장 다중샷 재생 | 20개 통과 |
| `flutter analyze` | 통과, 문제 없음 |
| `flutter build web --release` | 통과 |
| Web 루트 | HTTP 200 |
| Web `main.dart.js` | HTTP 200 |
| 데모 서버 | `127.0.0.1:8080`, PID 67695 |

Web 시각 캡처:

- `390x844`, `393x852`, `430x932`, `768x1024`, `1024x1366`
- 홈·섬 지도·플레이 상태 각 캡처
- 다섯 해상도 Chromium 콘솔 오류 0건
- 390×844 지도에서 4단계 카드·설명·잠금·안내 카드 시각 확인
- 최신 캡처에서 390×844 지도·플레이의 한글 문구와 보드·팝업 비겹침 확인

Web 기준 번들 체크섬:

```text
build/web/main.dart.js
5135a6a01233855c1d01d8281ad28dd96cc87e6ba10942ebf793e6f4b1a65426

build/web/index.html
8ee95f8e449a4c7187b68651f03bf7678f67e08a1d68029cc31b140e9f20ed57
```

## 초기 상태 스냅샷

재현 명령:

```bash
dart run tool/baseline_snapshot.dart
```

스냅샷 도구의 출력은 `schemaVersion: 1`, 논리 좌표 `360 x 560`, 4개 단계의 초기 `GameState`를 포함한다. 각 단계에는 공 생성 위치, 엔티티 ID·타입·위치·크기·속성·이동 가능 여부·고체 여부·히트박스 배율·반발값이 포함된다.

현재 단계:

| 단계 | 이름 | 공 시작점 | 대표 엔티티 |
|---:|---|---|---|
| 1 | 무거움 익히기 | `(56, 456)` | 상자, 무거운 돌, 홀, 벽 |
| 2 | 탄성 익히기 | `(58, 462)` | 젤리, 내부 벽, 홀, 외곽 벽 |
| 3 | 연쇄 문 열기 | `(56, 466)` | 스위치, 문, 점착판, 상자, 무거운 돌, 홀 |
| 4 | 풍선 터뜨리기 | `(56, 466)` | 풍선, 뾰족함 원본, 풍선 스위치·문, 상자, 홀 |

## 저장 기준

현재 진행 저장은 서버 없이 `SharedPreferences`를 사용한다.

| 키 | 의미 | 처리 상태 |
|---|---|---|
| `property_shot_cleared_levels` | 클리어한 단계 인덱스 문자열 목록 | 현재 해금 기준 |
| `property_shot_unlocked_level` | 지도 해금 최고 단계 | 현재 키·구버전 호환 |
| `unlocked_level` | 과거 게임 화면 해금 키 | 읽기·쓰기 호환 |
| `best_shots_level_<index>` | 단계별 최고 샷 수 | 현재 사용 |
| `bonus_goal_level_<index>` | 선택형 추가 도전 달성 | 현재 사용 |
| `property_shot_copy_core_count` | 복제 코어 수량 | 현재 사용 |
| `property_shot_copy_core_rewarded` | 보상 지급 여부 | 현재 사용 |

클리어 목록은 연속된 단계만 지도 해금으로 계산한다. 구버전 최고 해금 값이 있으면 그보다 앞선 단계를 클리어한 것으로 마이그레이션한다. 저장 버전 키와 손상 데이터 복구 정책은 다음 실험 단계에서 추가한다.

## 이벤트 스트림 기준

물리 이벤트는 `ShotImpact`, `ShotAnimationMove`, `PhysicsEvent`로 구성된다. 이벤트에는 대상 ID·타입·충돌 위치·법선·경로 인덱스·충격량·결과 속도가 포함된다. 애니메이션은 물리 이벤트의 순서를 재생하며, 홀 진입은 뒤 벽 충돌보다 우선한다.

현재 자동 검증으로 확인된 범위:

- 벽 고정과 벽 재충돌
- 공·공 충돌 및 이전 공의 홀 진입
- 상자·돌·젤리·점착판·풍선의 타입별 반응
- 스위치 작동과 문 열림 인과
- 30·60·120Hz 이벤트 시간축
- 연쇄 이동의 실제 충돌 이후 시작

## 검증하지 않은 항목

- 실제 iPhone 12·iPad Pro의 FPS·메모리·터치 지연·햅틱·사운드
- 초보 플레이테스터의 30초 조작 시작률과 규칙 설명률
- 4단계 세 경로의 실제 사용자 발견률
- Web과 iOS Release의 실행 결과 비교
- 저장 버전 도입 전 손상 데이터 복구
- iOS `--no-codesign`은 Xcode 컴파일 후 Development Team·프로비저닝 프로파일 부족으로 배포 단계 실패
- `physics_benchmark.dart` 최신 순수 판정 평균: 1단계 416.8µs, 2단계 329.8µs, 3단계 338.6µs, 4단계 354.9µs

이 항목들은 자동 테스트 통과로 완료 처리하지 않는다.
