# 검증 결과

실행 일시: 2026-08-01 KST

| 명령 | 결과 |
|---|---|
| `dart format --output=none --set-exit-if-changed .` | 통과, 변경 없음 |
| `flutter analyze` | 통과, 이슈 없음 |
| `flutter test` | 통과, 101개 |
| `flutter build web --release` | 통과 |
| `curl http://127.0.0.1:8080` | HTTP 200, 새 빌드 서버 |
| `curl http://127.0.0.1:8080/assets/assets/generated/jelly-bumper-v1.png` | HTTP 200, RGBA 젤리 스프라이트 |
| `flutter build ios --no-codesign` | Xcode 빌드 완료 후 Development Team/프로비저닝 프로파일 부족으로 배포 빌드 실패 |
| `flutter build ios --simulator` | Xcode 16.2 프로젝트 컴파일 완료, 설치된 iOS Simulator Runtime 없음으로 목적지 선택 실패 |

기존 8080 프로세스를 종료한 뒤 새 빌드 서버를 시작했고 HTTP 200 응답을 확인했다.

## 상용 수직 슬라이스 반복 추가 검증

- 보드 바깥 배경·목재 프레임·잔디 필드의 시각 계층을 추가했다.
- 조준 화살표와 파워 게이지의 색상·그림자·끝점 표시를 분리했다.
- 저파워·고파워 실패 원인 이벤트를 추가했다.
- 활성 공의 직접 충돌과 홀 포획을 `ShotImpact`로 기록했다.
- 충돌 위치·법선·대상·경로 시점 단위 테스트를 추가했다.
- 활성 공·연쇄 물체의 충돌을 `ShotImpact`로 통합하고, 연쇄 이동 경로를 누적 거리로 재생하도록 보정했다.
- `ShotImpact`에 실제 대상 ID를 추가하고 연쇄 공의 홀 선행 포획·벽 충돌 차단 회귀를 추가했다.
- 충돌 재질별 햅틱 강도와 진행률 기반 문 개방 연출을 연결했다.
- 첫 2단계 성공 입력을 2도·0.02 파워 간격으로 스캔하는 난이도 회귀를 추가했다. 연결된 성공 영역은 1단계 각도 16도·파워 0.86·최대 248셀, 2단계 각도 16도·파워 0.72·최대 223셀이다.
- 1·2단계의 홀 크기를 42 단위로 조정해 성공 영역을 넓혔고, 각도·파워 격자의 연결 성분을 자동 검증한다.
- 가상 30·60·120Hz 애니메이션 회귀에서 충돌 이벤트 중복 없음과 완료 콜백 단일 실행을 확인했다.
- 무거운 충돌은 플랫폼 알림음, 일반·탄성·점착 충돌은 플랫폼 클릭음으로 구분하는 매핑과 회귀를 추가했다.
- 젤리 범퍼를 생성 RGBA 스프라이트로 교체하고 게임 화면·물체 썸네일에 연결했다. 속성 보유 점멸은 이미지 렌더 경로에서도 유지하며, 젤리 원본 비율과 알파 가장자리를 보정했다.
- 하드 키 변환으로 젤리 하이라이트 내부 투명 구멍을 줄였고, 충돌 시 젤리 물결·방울 보조 연출을 추가했다. 최신 Web 빌드의 젤리 자산 응답도 HTTP 200으로 확인했다.
- 벽에 장난감 레일의 세로 이음새와 리벳을 추가하고 홀 깃발에 미세한 흔들림·하이라이트를 추가했다. 물리 판정과 히트박스는 변경하지 않았다.
- 조준 화살표를 분절 샤프트·채워진 화살촉으로 바꾸고 힘 게이지에 구간 표식을 추가했다. 최종 궤적은 노출하지 않으며 조준·힘의 역할을 분리한다.
- 복사 모델 결정 전 안전한 UI 개선으로 `떼어 공에 옮기기`와 `원본에 남기고 공에 복사하기` 문구를 적용했다. 복사 소모·복원 규칙과 물리 판정은 변경하지 않았다.
- 이전·복사 원본 결과 안내 위젯 회귀 테스트를 추가했으며 전체 테스트 기준은 100개로 갱신했다.
- 전체 테스트 100개 통과와 `flutter analyze` 통과를 확인했다.
- Playwright Chromium 캡처로 390x844와 1280x900 화면을 확인했다. 모바일 한글 안내·목표·오브젝트·하단 조작 영역의 말줄임·겹침이 없고, 데스크톱 보드 중앙 배치와 최종 궤적 비노출 조준이 유지된다.
- 같은 390x844 화면에서 돌을 실제 좌표 클릭해 속성 팝업을 열고 돌 아이콘·설명·이전·복사 버튼을 확인했다. Flutter Web 캔버스 접근성 DOM 직접 조회는 별도 미지원이며, 위젯 접근성 회귀로 보완한다.

## 2026-08-02 복사권 모델 A 검증

- 1·2단계 복사권을 1회, 3단계 복사권을 2회로 초기화했다.
- 복사 성공 때만 잔여 복사권이 줄고, 원본 속성은 유지된다.
- 복사권이 0이면 추가 복사가 차단되고, 잘못된 선택에서는 복사권이 줄지 않는다.
- 발사 후 되감기와 단계 초기화에서 초기 복사권이 복원된다.
- 속성 팝업과 복사 결과 메시지에 한글 `복사 n회 남음`을 표시한다.
- 복사를 사용하지 않는 기존 물리 경로와 기믹 우회 경로는 변경하지 않았다.
- `flutter analyze` 통과, 전체 Flutter 테스트 105개 통과.

## 2026-08-02 목표 재감사 및 축약 HUD 보강

- `flutter analyze`: 통과, 문제 없음.
- `flutter test`: 전체 105개 통과.
- `flutter build web --release`: 통과.
- `flutter build ios --no-codesign`: Swift Package Manager 의존성 해결 단계에서 실패. iOS 코드 오류나 서명 성공으로 기록하지 않는다.
- 320px 축약 HUD에 `1 방향 조정 · 2 공을 길게 누르기 · 3 손 떼기`를 추가하고 위젯 회귀를 보강했다.
- 독립 Subagent 공통 결론: 물리·기능은 조건부 통과, 실제 사용자 이해도·iPhone/iPad 프레임·GPU·메모리·햅틱은 미검증.
- 현재 전체 테스트 수는 105개다. 문서의 이전 101개 수치는 과거 기준선으로 남긴다.
- 이전 버튼까지 실제로 눌러 팝업 종료, 원본 선택 상태 해제, 공의 `공: 무거움` 표시 전환을 확인했다.
- 연결된 성공 영역과 가상 프레임 시간축은 자동 검증했지만 실제 초보자 이해도는 증명하지 않는다. Canvas 픽셀 골든, 실제 30·60·120FPS 프레임 측정, iOS 실기기, iPhone/iPad 햅틱도 아직 미검증이다.

## 2026-08-02 다중 풀이·표시 비율 재검증

- 3단계에서 속성 없이 `switch_pressed`·`sticky_attached`를 발생시키지 않고 `GamePhase.success`에 도달하는 경로를 2도·0.02 파워 격자로 자동 탐색했다.
- 추가 방향 다양성 탐색에서는 기믹 없는 경로가 한 조준 구간에 집중됨을 확인했다. 따라서 현재 다중 풀이 증거는 무거운 공의 스위치·문 경로와 기믹 없는 홀 경로라는 서로 다른 해법 계열로 기록하며, 우회 방향 폭은 실제 플레이테스트에서 조정할 항목으로 남긴다.
- 젤리 스프라이트의 표시 폭을 엔티티 물리 폭에 맞췄다. 작은 화면의 보드 최소 폭 계약은 기존 위젯 테스트로 다시 통과했다.
- 데스크톱 HUD의 조준 안내는 `화면을 움직여 조준하고, 공을 0.45초 이상 누른 뒤 손을 떼세요.`로 고정했다. 모바일에서는 별도 줄을 추가하지 않아 보드 우선 레이아웃을 보존한다.
- 최신 전체 회귀 기준은 105개이며, 실제 사용자 플레이테스트·iOS 실기기·Canvas 픽셀 골든은 미검증이다.
- 최신 Web release를 기존 8080 프로세스 종료 후 교체하고 Playwright로 390x844·1280x900을 캡처했다. 모바일 보드 최소 폭·한글 HUD·조준 UI의 겹침이 없고, 데스크톱 HUD의 손가락 아이콘 안내가 표시된다.
- 첫 단계 메시지에 발사 순서를 보강한 최신 Web 빌드를 다시 캡처했다. 390x844 화면에서 `공을 길게 눌렀다 손을 떼 발사` 문장이 잘리지 않고 보드 최소 폭도 유지된다.

`flutter build ios --no-codesign`은 Xcode 빌드 후 Development Team과 프로비저닝 프로파일 부족으로 실패했다. 서명 없는 시뮬레이터 빌드와 실제 기기 테스트는 별도 환경이 필요하다.

이번 검증에는 벽 네 방향 경계, 연쇄 벽 반사, 돌·상자·젤리 혼합 재질 연쇄 순서, 최신 위치 전달, 정확한 벽 모서리 대각선 법선, 벽 충돌 법선 시각 피드백, 상태가 비고체여도 벽을 물리 장애물로 처리하는 불변식, 미리보기와 실제 벽 후보 일치, 비정사각형 원형 히트박스, 반발값에 따른 상자 이동량 차이, 2단계 대체 경로 성공, 3단계 점착·직접 스위치 경로와 점착 없는 실제 홀 진입, 연쇄 스위치의 무거움 상호작용, 탄성 학습 단계의 일반 공 감속, 동일 질량 공 충돌, 점착 지속성, 홀 경계 성공·실패, 속성 없는 과거 공의 홀 포획, 연쇄 공 홀 선행 포획, 홀 포획 시각 상태 전달, 충돌 발생 주체 식별, 실패 복구 패널, 클리어 팝업 뒤로가기 복귀, 팝업 접근성 격리, 접근성 힘 조절, 320px 안내 말줄임 방지, 발사 중 정보 팝업 차단, 보드 우선 반응형 레이아웃, 앱 생명주기 충전 취소, 조기 롱프레스 취소, 안전영역 팝업 경계, HUD·컨트롤 비겹침, 휴대폰별 한글 접근성 의미, 기본 플랫폼 사운드·재질별 햅틱 연결, 재질별 플랫폼 사운드 구분, 연결된 각도·힘 격자 기반 단계별 풀이 존재, 가상 30·60·120Hz 애니메이션 시간축, 홀 진입 경로 우선, 고정 상자 이동 이벤트, 점착 재질별 첫 충돌, 상단 보드 배치와 첫 샷 안내 회귀, 이전·복사 원본 결과 안내, 3단계 기믹 우회 경로 탐색이 포함되며 과거 기준선에서는 101개, 최신 기준에서는 105개 테스트가 통과했다. 과거 95·96·97·98·99·100·101개 수치는 역사적 기준선이다.

## 2026-08-02 320px 안내 캡처

- 짧아진 첫 단계 메시지가 320x568 화면에서 두 줄 안에 표시된다.
- 보드와 하단 조작 영역은 기존 최소 폭 계약을 유지한다.
- 최신 Web release 서버의 루트와 `main.dart.js`는 HTTP 200이다.

## 2026-08-02 순수 물리 성능 기준선

- `dart run tool/physics_benchmark.dart`를 각 단계 1,000회 실행했다.
- 최신 실행에서 1단계 평균 312.6µs, 2단계 평균 215.2µs, 3단계 평균 280.3µs였다. 확인값은 각각 75521, 48110, 63726으로 이전 실행과 같다.
- 이 수치는 `ShotResolver`만 포함하며 Canvas 렌더링·이미지 메모리·실제 iPhone/iPad 프레임 시간은 포함하지 않는다.

최신 재실행 값은 1단계 330.8µs, 2단계 209.8µs, 3단계 286.7µs이며 확인값은 각각 75521, 48110, 63726이다. 위의 312.6/215.2/280.3µs는 이전 실행의 역사적 값이다.

## 2026-08-02 전체 플레이 화면 Golden 회귀 게이트

| 항목 | 결과 |
|---|---|
| Golden 테스트 | `test/game_screen_golden_test.dart` 추가, 390×844·768×1024 기준 이미지 생성 |
| 대상 범위 | 해변 배경·HUD·보드·벽·홀·공·물체·조준 표시·하단 컨트롤 |
| 테스트 실행 | 통과, 2개 |
| Golden 호스트 자산 로드 | `loadGameAssets: false`로 래스터 디코더 교착을 피하고 실제 `PropertyShotGame` Canvas 렌더·레이아웃을 캡처 |
| 제품 기본 경로 | 기본값 `true`로 기존 래스터 자산 로드 유지 |

첫 시도에서는 Flame 이미지 자산의 비동기 로드가 Flutter Golden 호스트에서 완료되지 않아 빈 배경이 기준 이미지로 생성될 위험이 있었다. 공식 `toBeLoaded()`를 확인한 뒤에도 이미지 디코더가 교착되어, 테스트 전용 주입값으로 래스터 로드만 끄고 보드 렌더러·HUD·레이아웃을 검증하도록 분리했다. 이 Golden은 최종 래스터 품질을 대신하지 않으며, 래스터 자산은 기존 Web 캡처와 자산 HTTP 200 검증으로 보완한다.

## 2026-08-02 Web 프레임·발사 성능 감사

`scripts/web_performance_audit.py`가 최신 Web release에서 390×844·768×1024 홈→플레이→발사 흐름을 측정해 `harness_docs/qa/web_performance_latest.json`으로 저장했다.

| 뷰포트 | 유휴 평균 / p95 | 발사 평균 / p95 | 20ms 초과 | 콘솔 오류 |
|---|---:|---:|---:|---:|
| 390×844 | 16.666ms / 16.7ms | 16.666ms / 16.8ms | 0% | 0건 |
| 768×1024 | 16.666ms / 16.7ms | 16.666ms / 16.8ms | 0% | 0건 |

측정 런타임은 Headless Chromium이며 실제 iPhone·iPad 프레임·터치 지연·GPU·메모리 결과가 아니다. 현재 샌드박스 재실행에서는 macOS Chromium 프로세스가 `thermal_state_observer` SIGTRAP으로 종료되어, 기존 성공 JSON을 최신 실기기 증거로 승격하지 않았다.

## 2026-08-02 섬 지도 진행 경로 회귀 게이트

| 항목 | 결과 |
|---|---|
| 지도 Golden | `test/stage_select_golden_test.dart` 추가, 390×844·768×1024 기준 이미지 생성 |
| 지도 UX | 연결 경로, 잠금·해금 진행률, 다음 항해 안내 추가 |
| 접근성 동작 | 실제 한 번 탭 시작과 한글 힌트 일치 |
| 관련 위젯 테스트 | 통과, 43개 |
| 전체 `flutter test` | 통과, 136개 |

지도 화면은 기존 단계 선택 키와 잠금 규칙을 유지하면서 카드 사이의 진행 경로와 `1 / 3` 진행 표시를 추가했다. `한 번의 발사, 여러 갈래의 길` 안내는 특정 기믹만 강제하지 않는 게임 설계를 지도 단계에서도 전달한다.

이번 반복의 최신 회귀 기준은 `flutter analyze` 통과와 `flutter test` 136개 통과다. 과거 섹션에 남은 105·119·128·131·133개 수치는 각 시점의 역사적 기준선이며 현재 판정에 사용하지 않는다.

## 2026-08-02 최신 iOS 빌드 재확인

| 명령 | 결과 |
|---|---|
| `flutter build ios --no-codesign` | Xcode 컴파일 통과, 배포 단계에서 Development Team·Provisioning Profile 부족으로 실패 |
| 실패 분류 | 프로젝트 코드 컴파일 실패가 아닌 서명·프로비저닝 환경 미완료 |
| 현재 제한 | 실제 iPhone/iPad 설치·터치·프레임·GPU·메모리·햅틱 측정 불가 |

빌드 로그의 타깃은 `com.example.propertyShot`이며 Apple Developer 계정과 고유 Bundle ID, Team 선택이 필요하다. 사용자가 실제 기기 검증 환경을 제공하기 전에는 iOS 출시 준비 완료로 표현하지 않는다.

## 2026-08-02 공 화풍 일관성 회귀

| 항목 | 결과 |
|---|---|
| 공 표현 | 홈 미리보기·속성 팝업·플레이 래스터 자산 Golden이 `GameBallIconPainter`의 얼굴 달린 구를 공유 |
| 레거시 농구공 | `assets/icons/ball.png`는 제품 화면에서 제거하고 권리대장에는 레거시 보관 자산으로 기록 |
| 시각 검증 | `raster_assets_golden_test.dart` 390×844·768×1024와 홈·플레이 Golden 재생성 |

이전 홈 화면의 작은 농구공과 실제 플레이 공의 식별성 차이를 제거했다. 상자·돌·젤리는 래스터 스프라이트를 유지하고, 공만 Canvas 공통 페인터로 통일한다.

## 2026-08-02 최신 최종 회귀

| 항목 | 결과 |
|---|---|
| `dart format` | 통과 |
| `flutter analyze` | 통과, 이슈 없음 |
| `flutter test` | 통과, 119개 |
| `dart run tool/difficulty_report.dart` | 통과, 단일·다중 샷 지표 출력 |
| `dart run tool/physics_benchmark.dart` | 통과, 1단계 331.4µs·2단계 311.3µs·3단계 295.5µs |
| `flutter build web --release` | 통과 |
| `flutter build web --release --wasm` | 통과 |
| 최신 8080 서버 루트·`main.dart.js` | HTTP 200, PID 58389 |
| Playwright 390×844 제품 경로 | 홈·지도·플레이·결과 캡처, 브라우저 콘솔 오류 0건 |
| `flutter build ios --no-codesign` | Xcode 컴파일 후 Development Team·프로비저닝 프로파일 부족으로 실패 |

추가된 다중 샷 분석기는 최대 2회 입력에서 이전 공·이동 물체·문 상태를 다음 상태로 전달하고, 후속 발사마다 유지·남은 속성 이전·조건부 복제를 다시 선택해 행동 순서를 기록한다. 1·2단계 홀 크기 조정 후 고해상도 보조 분석에서 1단계 연속 조준 폭 28도, 2단계 20도를 확인했다. 3단계에서는 `점착 이전 → 무거움 이전`과 `점착 복제 → 무거움 이전` 2회 성공 시퀀스를 확인했다.

실제 iPhone·iPad 터치, 프레임·GPU·메모리, 햅틱·사운드 체감, 실제 사용자 이해도는 이 환경에서 검증하지 않았다. 점착 최종 성공은 자동 물리 탐색으로 확인했으나 실기기·실사용자 플레이 결과로 확정한 것은 아니다.

최신 Web release에는 OFL Nanum Gothic 정규·굵은·ExtraBold 글꼴을 번들했으며, 390×844 캡처에서 한글·공·오브젝트·결과 팝업을 다시 확인했다.

## 2026-08-02 데모 교체 스크립트

- `./scripts/run_web_demo.sh`가 8080의 기존 `http.server`를 확인·종료한다.
- 최신 `flutter build web --release`를 수행한다.
- 새 서버를 포그라운드로 유지하고 루트·`main.dart.js` HTTP 200을 검증한다.
- 실제 실행에서 PID 77684를 유지하는 동안 별도 curl로 두 응답 모두 200을 확인했다. `Ctrl+C` 종료 정리도 스크립트에 포함했다.

최신 실행에서는 기존 PID 43498을 종료하고 PID 62251로 교체했으며 승인된 환경의 별도 `curl`에서 루트와 `main.dart.js` 모두 HTTP 200을 확인했다.

## 2026-08-02 최종 축약 안내

- 축약 HUD에 별도 행을 추가하지 않고 초기 `compact_message`를 `방향 조정 → 공 길게 누르기 → 손 떼기`로 짧게 바꿨다.
- 320x568 위젯에서 보드 폭 240 이상, 축약 메시지 존재·문구, 예외 없음이 통과했다.
- 넓은 화면 안내는 `손가락을 움직여 조준하고...`로 정리했다.

## 2026-08-02 최신 모바일 셸·결과 화면 회귀

| 항목 | 결과 |
|---|---|
| `dart format --output=none --set-exit-if-changed .` | 통과 |
| `flutter analyze` | 통과, 이슈 없음 |
| `flutter test` | 통과, 124개 |
| `flutter test test/widget_test.dart` | 통과, 41개. 결과 패널 높이·작은 화면 버튼·다음 단계 이동·축약 라우터·계측 포함 |
| `flutter build web --release` | 통과 |
| 데모 서버 교체 | 기존 PID 14419 종료 후 최신 PID 26180으로 교체 |
| 서버 응답 | `http://127.0.0.1:8080/` 및 `main.dart.js` HTTP 200 |
| Web 브라우저 콘솔 | 최신 제품 경로 캡처에서 오류 0건 |
| 화면 캡처 | 390×844 홈·지도·플레이·결과, 393×852·430×932·768×1024·1024×1366 플레이 |
| `flutter build ios --no-codesign` | Xcode 컴파일 완료, Development Team·프로비저닝 프로파일 부족으로 배포 단계 실패 |

이번 반복에서는 모바일 플레이 보드의 컴팩트 좌우 여백을 제거해 390×844에서 보드가 약 605px 높이로 표시되도록 했고, 결과 팝업을 최대 540px 콘텐츠 패널로 축소했다. 결과 정보 영역만 스크롤되고 `다음`·`기록 다시 도전` 버튼은 하단에 고정된다. 클리어 패널의 실제 높이와 버튼 경계는 위젯 회귀로 검증했다. 홈에는 공·돌·상자·홀을 함께 보여주는 보드 미리보기를 추가했다.

실제 iPhone·iPad 터치, 60fps·GPU·메모리, 사운드·햅틱 체감과 사용자 이해도는 여전히 미검증이다. `390x844-clear-attempt.png`는 의도된 실패 피드백 캡처이며, 상용 경로 증거에는 `390x844-result.png`를 사용한다.

## 2026-08-02 최신 실행 확인

| 항목 | 결과 |
|---|---|
| `flutter analyze` | 통과, 이슈 없음 |
| `flutter test` | 통과, 124개 |
| 최신 데모 서버 | 기존 프로세스 종료 후 PID 33893으로 8080 유지 |
| 서버 응답 | 루트 HTTP 200, `main.dart.js` HTTP 200 |
| 390×844 실브라우저 | 홈·플레이 진입·속성 보유 돌 상세 팝업 확인 |
| 브라우저 콘솔 | 오류 0건 |

이번 확인은 변경된 물리 판정이 아니라 최신 빌드의 실행 가능성과 핵심 첫 사용자 흐름을 재검증한 것이다. 실제 기기 프레임·GPU·메모리와 사용자 이해도는 여전히 별도 검증이 필요하다.

## 2026-08-02 최신 아트·축약 HUD 반복

| 항목 | 결과 |
|---|---|
| `dart format --output=none --set-exit-if-changed .` | 통과, 26개 파일 검사 |
| `flutter analyze` | 통과, 이슈 없음 |
| `flutter test` | 통과, 124개 |
| `flutter test test/widget_test.dart` | 통과, 41개 |
| `dart run tool/difficulty_report.dart` | 통과, 1단계 28도·86%, 2단계 20도·76%, 3단계 무속성·무거움 대체 풀이 확인 |
| `dart run tool/physics_benchmark.dart` | 통과, 1단계 343.7µs·2단계 287.8µs·3단계 331.7µs |
| `flutter build web --release` | 통과 |
| 데모 서버 교체 | 기존 PID 43723 종료 후 PID 55418으로 교체 |
| 서버 응답 | 루트 HTTP 200, `main.dart.js` HTTP 200 |
| 실브라우저 캡처 | 390×844·430×932·768×1024 실행, 콘솔 오류 0건 |
| `flutter build ios --no-codesign` | Xcode 컴파일 완료, Development Team·프로비저닝 프로파일 부족으로 배포 단계 실패 |

이번 반복의 코드 변경은 물리 좌표·충돌 계산을 건드리지 않고, 벽을 해변 장난감 경계처럼 보이게 하는 재질 디테일, 공과 연결된 산호색 조준 리본, 제품 축약 HUD의 한 줄 목표, 보드 밖 해안선·모래결·조개 장식에 한정했다. 최신 캡처에서 390px HUD 압박과 단색 하단 여백은 완화됐지만, Canvas와 래스터 에셋의 질감 완전 통일과 실제 기기 검증은 남아 있다.

실제 iPhone·iPad 터치, 60fps·GPU·메모리, 햅틱·사운드 체감, 실제 사용자 30초·5분 이해도는 여전히 검증하지 않았다.

최신 390×844 Web 흐름에서 보드의 돌을 실제 탭해 속성 상세 팝업을 열고, `떼어 공에 옮기기`를 탭해 공의 `무거움` 상태와 축약 안내 문구가 갱신되는 것을 확인했다. 이 흐름의 캡처는 `390x844-material-source-popup.png`와 `390x844-material-transfer-after.png`다.

최종 재실행에서는 축약 3단계 진행 힌트도 줄임표 없이 `무거움은 스위치 · 점착은 공 고정`처럼 상태별 핵심 문장으로 표시되도록 확인했고, 전체 `flutter test`는 다시 124개 통과했다. 최신 데모 PID는 61743이며 루트와 `main.dart.js`는 HTTP 200이다.

`flutter build ios --simulator`도 최신 코드에서 재실행했다. Xcode 컴파일 단계까지 진행됐으나 iOS 18.2 Simulator Runtime이 설치되지 않아 `generic:1, platform:iOS Simulator` 대상 선택 단계에서 실패했다. 따라서 현재 iOS 상태는 디바이스 배포 서명 부족과 시뮬레이터 런타임 부족이 서로 다른 환경 문제로 남아 있다.

## 2026-08-02 Canvas 표면 마감 반복

| 항목 | 결과 |
|---|---|
| `dart format lib/game/property_shot_game.dart` | 통과, 변경 파일 포맷 완료 |
| `git diff --check` | 통과 |
| `flutter analyze` | 통과, 이슈 없음 |
| `flutter test` | 통과, 124개 |
| `dart run tool/difficulty_report.dart` | 통과, 1단계 28도·86%, 2단계 20도·76%, 3단계 무속성·무거움 대체 풀이 확인 |
| `dart run tool/physics_benchmark.dart` | 통과, 1단계 590.1µs·2단계 576.7µs·3단계 511.2µs |
| Web release 빌드 | 통과 |
| 데모 서버 교체 | 기존 PID 61743 종료 후 PID 71571로 8080 교체 |
| 서버 응답 | 루트 HTTP 200, `main.dart.js` HTTP 200 |
| 390×844 실제 시작 입력 | 플레이 화면 진입, 콘솔 오류 0건 |
| 390×844·768×1024 캡처 | `390x844-canvas-finish-play.png`, `390x844-canvas-finish.png`, `768x1024-canvas-finish.png` 생성 |

이번 반복은 물리 좌표·히트박스·충돌 계산을 변경하지 않고 Canvas 도형에 공통 왼쪽 위 광원, 하단 음영, 상단·하단 에지 마감을 낮은 불투명도로 추가했다. 390px 플레이 캡처에서 벽의 상단 광원과 하단 접지감이 기존 돌·상자 래스터의 압출·윤곽 처리와 함께 보이며, HUD 한글 문장과 보드 경계가 겹치지 않는다.

Playwright WebKit 런타임이 설치되어 있지 않아 모바일 캡처는 Chromium 390×844 뷰포트로 대체했다. 실제 iPhone·iPad 터치, 60fps·GPU·메모리, 햅틱·사운드 체감과 실제 사용자 이해도는 여전히 검증하지 않았다.

## 2026-08-02 래스터 표면 마감 반복

| 항목 | 결과 |
|---|---|
| `dart format lib/game/property_shot_game.dart` | 통과 |
| `git diff --check` | 통과 |
| `flutter analyze` | 통과, 이슈 없음 |
| `flutter test` | 통과, 124개 |
| `dart run tool/physics_benchmark.dart` | 통과, 1단계 344.1µs·2단계 282.8µs·3단계 300.7µs |
| `flutter build web --release` | 통과 |
| 데모 서버 교체 | 기존 PID 71571 종료 후 PID 81493으로 교체 |
| 서버 응답 | 루트 HTTP 200, `main.dart.js` HTTP 200 |
| 390×844 실제 플레이 | 시작 입력 후 캡처, 콘솔 오류 0건 |
| 768×1024 실제 플레이 | 시작 입력 후 캡처, 콘솔 오류 0건 |
| `flutter build ios --no-codesign` | Xcode 컴파일 완료 후 Development Team·프로비저닝 프로파일 부족으로 실패 |

이번 반복은 기존 돌·상자·젤리 래스터 질감과 압출을 유지하면서 Canvas 도형과 동일한 광원 방향·하단 음영·상단·하단 에지 토큰을 `srcATop` 표면 마감으로 통합했다. 물리 좌표·히트박스·충돌 계산은 변경하지 않았다.

새 캡처는 `390x844-raster-finish-play.png`, `768x1024-raster-finish-play.png`이며 두 화면 모두 한글 HUD·보드·하단 조작 영역이 겹치지 않는다. WebKit 런타임은 설치되어 있지 않아 Chromium 뷰포트로 검증했으며, 실제 iPhone/iPad 프레임·GPU·메모리·햅틱·사운드 체감은 미검증이다.

Turing 독립 사후 QA는 P0 없이 조건부 통과로 판정했다. 한글 UI·모바일 레이아웃·오브젝트 식별성과 물리 불변은 통과했지만, bitmap 질감과 Canvas 벽의 완전한 재질 통일은 P1, 하단 모래 영역의 낮은 정보 밀도는 P2로 남겼다.

## 2026-08-02 하단 해변 세계 연결 반복

| 항목 | 결과 |
|---|---|
| `dart format lib/ui/game_screen.dart` | 통과 |
| `git diff --check` | 통과 |
| `flutter analyze` | 통과, 이슈 없음 |
| `flutter test` | 통과, 124개 |
| Web release 빌드 | 통과 |
| 데모 서버 교체 | 기존 PID 81493 종료 후 PID 89192로 교체 |
| 서버 응답 | 루트 HTTP 200, `main.dart.js` HTTP 200 |
| 390×844 실제 플레이 | 하단 조수 웅덩이·잎사귀·불가사리 확인, 콘솔 오류 0건 |
| 768×1024 실제 플레이 | 보드·HUD·입력 영역 유지, 콘솔 오류 0건 |

보드 바깥 하단 모래 영역에 낮은 대비의 해변 장식을 추가했다. 장식은 `CustomPainter` 배경에만 그려지며 충돌·입력 대상이 아니다. 390px에서 단색 여백 인상이 줄었고, 768px에서는 큰 보드 뒤의 배경 장식으로 물러난다.

Hooke 독립 사후 QA는 P0·P1 없이 통과했다. 390·768px 간 장식 노출 차이와 반복 패턴은 P2 잔여 위험으로 기록했으며, 기능 UI·물리·입력 침범은 발견되지 않았다.

## 2026-08-02 Golden 화면 회귀 게이트

| 항목 | 결과 |
|---|---|
| Golden 테스트 | `test/gameplay_backdrop_golden_test.dart` 추가, 390×844·768×1024 기준 이미지 생성 |
| `flutter test test/gameplay_backdrop_golden_test.dart` | 통과, 2개 |
| `flutter test` | 통과, 126개 |
| `dart format --output=none --set-exit-if-changed .` | 통과, 27개 파일 검사 |
| `flutter analyze` | 통과, 이슈 없음 |
| Web release 빌드 | 통과 |
| 데모 서버 교체 | 기존 PID 89192 종료 후 PID 96760으로 교체 |
| 390×844·768×1024 실제 플레이 | 캡처 생성, 브라우저 콘솔 오류 0건 |

`GameplayBackdropPainter`를 픽셀 회귀 대상에 고정해 해변 배경의 파도·모래 경계·하단 장식이 의도 없이 바뀌면 테스트가 실패하도록 했다. Golden은 배경 레이어를 검증하며 실제 iPhone/iPad 렌더링·프레임·GPU·메모리 증거를 대신하지 않는다.

Arendt 독립 QA는 P0 없이 통과했으며, Golden 범위가 배경 페인터 목적에는 적절하다고 평가했다. 다만 상·하단 HUD, 보드 배치, 한글 텍스트는 자동 Golden 대상이 아니므로 실제 브라우저 캡처와 위젯·레이아웃 테스트가 보완 증거이며, 전체 화면 픽셀 회귀 게이트는 아직 P1 잔여 위험이다.

## 2026-08-02 홈 화면 Golden 보강

| 항목 | 결과 |
|---|---|
| 홈 화면 Golden | `test/home_screen_golden_test.dart` 추가, 390×844·768×1024 기준 이미지 생성 |
| 테스트 폰트 | NanumGothic 정규·굵은·ExtraBold와 MaterialIcons를 명시 로드 |
| 홈 Golden 테스트 | 통과, 2개 |
| `flutter test` | 통과, 128개 |
| `flutter analyze` | 통과, 이슈 없음 |
| 에셋 준비 | `stone-v2.png`·`crate-v2.png`·`ball.png`를 `runAsync` + `precacheImage`로 명시 준비 |

첫 이미지 생성에서 Flutter 테스트 환경의 폰트 로딩이 Web 캡처와 달라 한글·아이콘이 네모로 보이는 문제를 발견했다. 테스트 전용 폰트 로더를 보강한 뒤 기준 이미지를 재생성했고, 이제 홈의 한글 문구·버튼·아이콘·보드 미리보기를 자동 픽셀 회귀 대상으로 보호한다.

최신 Web release는 기존 PID 96760 종료 후 PID 10257로 교체했다. `390x844-home-font-gate.png`와 `768x1024-home-font-gate.png`에서 한글·Material 아이콘·보드 미리보기를 다시 확인했고 두 브라우저 콘솔 오류는 0건이었다.

추가 검증에서 Flutter Golden의 `FilledButton` 라벨이 테마 폰트를 상속하지 않아 한글이 깨지는 회귀를 발견했다. `FilledButton` 스타일에 현재 테마 폰트 패밀리를 명시한 뒤 Golden 기준 이미지와 실제 Web 홈 캡처를 다시 생성했으며, 시작 버튼의 `첫 섬에서 시작하기` 라벨이 정상 표시된다.

Carver의 독립 QA에서 처음 390px Golden만 이미지 로딩 전에 캡처되어 보드 오브젝트가 빠지는 순서 의존성을 P1로 보고했다. 이미지 세 자산을 명시적으로 precache하고 디코딩 완료 후 다시 펌프하도록 수정했으며, 재생성된 390px·768px 기준 이미지 모두 돌·상자·공·홀을 포함한다. 실제 Web release도 기존 PID 10257 종료 후 PID 25735로 교체했고 두 홈 캡처의 콘솔 오류는 0건이다.

Kant 독립 QA는 최종 두 해상도에서 오브젝트·홀·깃발·한글·Material 아이콘과 콘텐츠 겹침을 확인해 P0·P1 없음으로 판정했다. Golden과 실제 Web 캡처의 주요 콘텐츠가 약 8px 어긋나는 차이는 Flutter 테스트 렌더러와 Chromium의 안전영역·텍스트 배치 차이로 보이며 P2로 남긴다. Golden은 자동 픽셀 회귀, Web 캡처는 실제 브라우저 사용성 증거로 역할을 분리한다.

## 2026-08-02 모바일 HUD 안내 축약

| 항목 | 결과 |
|---|---|
| 변경 | 첫 시작 상태 메시지에서 단계 목표 중복을 제거하고 조작 순서만 표시 |
| `flutter test test/widget_test.dart` | 통과, 41개 |
| `flutter test` | 통과, 128개 |
| `flutter analyze` | 통과, 이슈 없음 |
| Web release | 통과 |
| 실제 플레이 캡처 | 390×844·393×852·430×932·768×1024·1024×1366 |
| 브라우저 콘솔 | 다섯 뷰포트 모두 오류 0건 |
| 데모 서버 | 기존 PID 25735 종료 후 PID 36420으로 교체, 루트·번들 HTTP 200 |

변경 전 390px 캡처에서는 `무거움으로 상자를 밀어 홀로 보내기`와 `상자를 밀어 홀로 → 방향 조정 · 길게 누르기 · 손 떼기`가 목표·조작 안내로 중복되었다. 변경 후 목표는 첫 줄에, 조작 순서는 둘째 줄에 분리되어 읽힌다. Gauss 독립 QA는 P0·P1 없이 통과했으며, 768px HUD 경계의 자동화가 캡처 육안 확인에 의존하는 점을 P2로 기록한다. 이 검증은 실제 사용자나 iOS 기기 결과가 아니다.

후속으로 HUD·컨트롤 경계 위젯 회귀를 768×1024까지 확장했다. 320×568·390×844·768×1024에서 HUD·보드·하단 컨트롤·되감기·초기화의 화면 내 위치와 겹침을 자동 확인하며, `flutter test test/widget_test.dart` 41개가 통과한다. 따라서 해당 P2 자동화 공백은 닫혔지만, 실제 기기 성능·터치 지연 검증은 여전히 별도 항목이다.

## 2026-08-02 연쇄 애니메이션 인과 순서 게이트

`test/animation_timeline_test.dart`의 30·60·120Hz 회귀에 `onAnimationImpact` 이동 이벤트의 `triggerPathIndex`가 비감소 순서로 전달되는지 추가 검증했다. 충돌 영향 콜백의 중복 없음과 애니메이션 완료 단일 실행을 함께 유지하며 3개 테스트가 통과한다. 이는 실제 프레임 시간·기기 체감의 대체가 아니라 결정론적 이벤트 시간축 검증이다.

## 2026-08-02 피드백 설정 저장과 문 열림 피드백

| 항목 | 결과 |
|---|---|
| 소리·진동 설정 로드/저장 | SharedPreferences 키를 통한 복원·기록 테스트 통과 |
| 저장소 예외 | 복제 코어 로딩 실패 시 기본 진행 상태로 홈 유지 |
| 문 열림 피드백 | `opening` 이벤트에 알림 사운드와 중간 햅틱 연결 |
| 관련 테스트 | `game_feedback_test.dart`·`widget_test.dart` 합계 46개 통과 |
| 전체 `flutter test` | 통과, 131개 |
| 정적 분석 | 통과, 이슈 없음 |
| 실제 Web 설정 팝업 | 390×844 캡처, 콘솔 오류 0건 |
| 새로고침 복원 | 효과음 끔 상태를 재실행 후 다시 확인, 콘솔 오류 0건 |

실제 Web 캡처는 `390x844-feedback-settings.png`와 `390x844-feedback-persisted.png`이며, 플랫폼 음량·무음 모드·실제 iPhone 햅틱 체감은 여전히 미검증이다.

## 2026-08-02 공 렌더링 공통화 후 최신 회귀

| 항목 | 결과 |
|---|---|
| 공 렌더링 | 홈 미리보기·속성 팝업·실제 Flame 플레이 공이 `lib/ui/game_ball_painter.dart`의 `GameBallIconPainter`를 공유 |
| 래스터 자산 Golden | 390×844·768×1024에서 돌·상자·젤리와 얼굴 공 확인 |
| 관련 회귀 | 래스터 Codec·자산 Golden·홈·플레이 Golden 대상 6개 통과 |
| 전체 회귀 | 최신 `flutter test` 139개 통과 |
| 정적 분석 | `flutter analyze` 통과, 이슈 없음 |

공통 기본 공의 구체·광택·표정을 하나의 페인터로 통일했고, 게임 전용 속성 질감과 충격 링은 기본 표현 위에 유지한다. `assets/icons/ball.png`는 제품 화면에서 사용하지 않는 CC0 레거시 보관 자산으로 권리대장에 기록했다.

## 2026-08-02 벽 재질 시각 회귀

| 항목 | 결과 |
|---|---|
| 변경 범위 | 벽의 Canvas 색상·깊이 면·표면 이음새·내부 가장자리만 변경 |
| 물리 보존 | `EntityState`·벽 히트박스·`ShotResolver` 변경 없음 |
| 화면 검증 | 390×844·768×1024 플레이 Golden과 해변 배경 Golden 통과 |
| 정적 분석 | `flutter analyze` 통과, 이슈 없음 |

반복 타일형 산업용 레일 인상을 줄이고 따뜻한 상단 캡, 낮은 빈도의 장난감 보드 이음새, 모래색 내부 가장자리로 해변 실험 섬의 고정 경계를 강화했다. 실제 Web·실기기 색감과 사용자 인식은 별도 검증 대상이다.

## 2026-08-02 재질별 피드백 큐

| 항목 | 결과 |
|---|---|
| 피드백 큐 | 발사·가벼운 충돌·무거운 충돌·탄성·점착·스위치·문·성공·실패를 구분 |
| 모바일 경로 | 기존 플랫폼 기본 소리와 햅틱 유지 |
| Web 경로 | 외부 음원 없이 조건부 Web Audio 합성 톤 구현 |
| 관련 테스트 | `game_feedback_test.dart` 통과 |
| 전체 회귀 | `flutter test` 140개 통과 |
| 정적 분석 | `dart analyze lib test` 이슈 없음 |
| Web release | 엔진 캐시 `engine.stamp`·`engine.realm` 쓰기 권한 오류로 미실행 |
| 데모 서버 | Web release 재빌드 전 상태에서 기록한 항목이며, 후속 최신 Web release 교체로 갱신됨 |

오디오 실패는 자동재생 정책·플랫폼 미지원·무음 모드에서 게임 상태를 중단하지 않도록 흡수한다. 실제 브라우저 청취와 iOS 햅틱 체감은 아직 확인하지 않았으며, 후속 Web release 교체로 최신 오디오 코드가 데모 서버에 반영됐다.

## 2026-08-02 최신 Web release 교체 및 프레임 감사

| 항목 | 결과 |
|---|---|
| 빌드 | `flutter build web --release` 통과 |
| 서버 교체 | 기존 PID 21972 종료 후 PID 47804로 교체 |
| HTTP | 루트·`main.dart.js` 모두 200 |
| Chromium 콘솔 | 390×844·768×1024 모두 오류 0건 |
| 프레임 시간 | 두 해상도 idle·발사 구간 평균 약 16.67ms, 20ms 초과 0% |
| 변경 후 캡처 | `390x844-audio-release-home/play.png`, `768x1024-audio-release-home/play.png` |

이 감사는 실제 iPhone·iPad 성능 결과가 아니라 Chromium Web release 보조 측정이다. 최신 오디오 코드가 포함된 번들에서 모바일 세로 셸, 공·바위·상자·홀·하단 조작 영역이 정상 렌더링되는 것을 캡처로 확인했다.

최종 유지 세션에서 동일한 `build/web`을 PID 81248로 실행했으며, 루트와 `main.dart.js`가 각각 HTTP 200이다. 서버 세션은 사용자가 데모를 확인할 수 있도록 유지한다.

## 2026-08-02 현재 iOS 빌드 진단

| 항목 | 결과 |
|---|---|
| 명령 | `flutter build ios --no-codesign` |
| Xcode 컴파일 | 완료 |
| 실패 단계 | 배포 가능한 iOS 앱 생성 단계 |
| 원인 | `Development Team`과 `Provisioning Profile` 미설정 |
| 코드 오류 여부 | 이번 변경의 Dart·Xcode 컴파일 오류는 확인되지 않음 |
| 다음 외부 조건 | Xcode Apple ID 로그인, 고유 Bundle ID, 팀·프로파일 선택 |

서명 설정은 저장소에서 임의로 만들 수 없는 Apple 개발자 계정 상태에 의존한다. 서명 없는 시뮬레이터·실기기 설치 결과와 실제 햅틱은 별도 검증으로 남긴다.

## 2026-08-02 첫 3단계 플레이 Golden 확장

| 항목 | 결과 |
|---|---|
| 단계 범위 | 1·2·3단계 전체 플레이 화면 |
| 해상도 | 390×844·768×1024 |
| Golden 기준 | 총 6개 생성 및 통과 |
| 2단계 시각 게이트 | 젤리와 반사벽 배치 확인 |
| 3단계 시각 게이트 | 스위치·문·점착판·상자·홀 배치 확인 |
| 정적 분석 | `flutter analyze` 무이슈 |
| 전체 회귀 | `flutter test` 144개 통과 |

이번 Golden은 실제 플레이 성공률이나 실제 사용자 이해도를 증명하지 않는다. 첫 3단계의 배치·한글 HUD·보드 경계·기믹 식별을 자동 픽셀 회귀로 보호하는 증거다.

독립 QA가 실제 래스터 경로 공백을 P1으로 지적해 `loadGameAssets: true` 재생성을 시도했으나, Flame `toBeLoaded()`가 1단계 첫 케이스에서 66초 동안 완료되지 않아 테스트 교착으로 판정했다. 따라서 플레이 Golden은 녹색 배치 게이트로 유지하고, 실제 래스터는 PNG·Codec·래스터 자산 Golden으로 별도 검증한다. 이 분리는 전체 게임 화면과 실제 Flame 자산이 한 테스트에서 동시에 검증됐다는 의미가 아니다.

## 2026-08-02 별·메달 보상 피드백

| 항목 | 결과 |
|---|---|
| 이벤트 | 클리어 별 계산 직후 `medal_awarded` 이벤트를 성공 이벤트와 분리 |
| Web 사운드 | 별·메달 전용 삼각파 합성음 큐 연결 |
| 모바일 피드백 | 별·메달 획득 시 가벼운 햅틱 연결 |
| 설정·예외 | 기존 소리·진동 토글과 미지원 플랫폼 예외 흡수 유지 |
| 관련 테스트 | `game_feedback_test.dart` 메달 큐 회귀 포함 |
| 전체 회귀 | `flutter test` 145개 통과 |
| 정적 분석 | `flutter analyze` 통과, 이슈 없음 |

별·메달 피드백은 클리어 결과의 별 계산 이후에만 발생하며, 실제 iOS 기기에서의 음량·햅틱 체감은 기존과 같이 별도 검증 대상이다.

## 2026-08-02 별·메달 피드백 포함 최신 Web 회귀

| 항목 | 결과 |
|---|---|
| 기존 서버 교체 | PID 95764 종료 후 최신 Web release PID 4815 시작 |
| HTTP | 루트·`main.dart.js` HTTP 200 |
| Chromium 실행 | 390×844·768×1024 콘솔 오류 0건 |
| 프레임 시간 | idle·발사 평균 약 16.67ms, 20ms 초과 0% |
| 캡처 | `390x844-medal-feedback-release-home/play.png`, `768x1024-medal-feedback-release-home/play.png` |
| 전체 회귀 | `flutter test` 145개 통과 |

최신 캡처는 보상 피드백 코드가 포함된 번들의 홈·플레이 화면이 한글 HUD, 해변 보드, 공·홀·속성 물체와 함께 정상 렌더링되는지 확인하는 Web 증거다. Chromium 수치는 실제 iPhone/iPad GPU·메모리·터치·햅틱 결과가 아니다.

## 2026-08-02 세부 피드백 큐와 오디오 순서 회귀

| 항목 | 결과 |
|---|---|
| 전용 큐 | 복제 코어 획득·조준 충전·젤리·홀 진입을 별도 큐로 분리 |
| 호환성 | 기존 탄성 충돌 큐와 재질별 햅틱 계약 유지 |
| 순서 | 홀·클리어·메달 등 연속 사운드 이벤트를 오디오 큐에서 순차 재생 |
| 디바운스 | 동일 재질의 70ms 중복 이벤트는 과도한 중첩음을 막기 위해 기존 정책 유지 |
| 테스트 | 피드백 관련 테스트 9개 통과, 오디오 중첩·순서 회귀 포함 |
| 전체 회귀 | `flutter test` 148개 통과 |
| 정적 분석 | `flutter analyze` 통과, 이슈 없음 |
| Web release | 빌드 통과, 기존 PID 4815 종료 후 PID 35285로 교체 |
| 브라우저 | 390×844·768×1024 콘솔 오류 0건, idle·발사 평균 약 16.67ms |
| 캡처 | `390x844-feedback-queue-release-home/play.png`, `768x1024-feedback-queue-release-home/play.png` |
| iOS | Xcode 컴파일 완료, Development Team·Provisioning Profile 부족으로 배포 단계 실패 |

오디오 큐는 게임 상태·물리·햅틱을 기다리게 하지 않으며, 브라우저 자동 재생 정책이나 실제 iOS 음량·햅틱 품질은 별도 검증 대상이다.

## 2026-08-02 선택형 추가 도전 회귀

| 항목 | 결과 |
|---|---|
| 추가 도전 | 1단계 3회 이하, 2단계 젤리 충돌, 3단계 스위치 작동 |
| 성공 조건과의 관계 | 선택형 기록이며 본 목표 성공·다음 단계 이동을 제한하지 않음 |
| 저장 | `SharedPreferences` 단계별 기록, 재실행 후 결과 팝업 표시 |
| 단위 테스트 | 조건 경계 3개 통과 |
| 위젯 테스트 | 저장된 추가 도전 달성 상태 팝업 표시 통과 |
| 전체 회귀 | `flutter test` 153개 통과 |
| 정적 분석 | `flutter analyze` 통과 |
| Web release | 되감기 보정 포함 빌드 통과, 기존 PID 74405 종료 후 PID 84151로 교체 |
| HTTP | 루트·`main.dart.js` 모두 200 |
| Chromium 콘솔 | 390×844·768×1024 모두 오류 0건 |
| 프레임 시간 | idle·발사 평균 약 16.67ms, p95 최대 16.8ms, 20ms 초과 0% |
| 캡처 | `390x844-bonus-goal-release-home.png`, `390x844-bonus-goal-release-play.png`, `768x1024-bonus-goal-release-home.png`, `768x1024-bonus-goal-release-play.png` |
| iOS | Xcode 컴파일 완료, Development Team·Provisioning Profile 부족으로 배포 단계 실패 |

추가 도전은 로컬 진행 피드백이며 온라인 리더보드나 사용자 성과로 해석하지 않는다. 독립 QA가 발견한 되감기 후 누적 플래그 잔존 P1은 발사 전 상태 이력 복원으로 해결했고, 관련 회귀를 추가했다. Web 수치는 실제 iPhone/iPad GPU·메모리·터치·햅틱 결과가 아니다.

## 2026-08-02 항해 지도·첫 행동 코치마크 회귀

| 항목 | 결과 |
|---|---|
| 지도 셸 | 세 스테이지를 좌우 섬 노드와 곡선 항해 경로로 배치, 목록형 카드 인상 완화 |
| 첫 행동 안내 | 1단계 바위 선택 코치마크 → 속성 이전 후 공 충전 코치마크 순서 |
| 상태 일치 | 공 속성 장착 뒤 하단 문구도 `공을 길게 눌러 힘을 모으세요`로 교체 |
| 골든 | 지도 2장·전체 플레이 6장 갱신 및 통과 |
| 위젯 회귀 | 코치마크 두 상태와 속성 이전 흐름 포함 |
| 전체 회귀 | `flutter test` 153개 통과 |
| 정적 분석 | `flutter analyze` 통과 |
| Web release | 경로 대비 보정 포함, 기존 PID 18554 종료 후 최신 PID 27962로 교체 |
| Chromium 콘솔 | 390×844·768×1024 모두 오류 0건 |
| 프레임 시간 | idle·발사 평균 약 16.67ms, p95 최대 16.8ms, 20ms 초과 0% |
| 변경 전 캡처 | `390x844-map.png`, `390x844-play.png` |
| 변경 후 캡처 | `390x844-stage-route-coach-release.png`, `390x844-tutorial-coach-release.png`, `390x844-tutorial-ball-coach-release.png`, `768x1024-stage-route-coach-release.png`, `768x1024-tutorial-coach-release.png` |

코치마크는 발견성을 보조하는 Web·위젯 증거이며, 실제 초보자의 30초·5분 이해도나 iPhone/iPad 터치·햅틱을 증명하지 않는다. 최종 iOS 진단은 Xcode 컴파일 완료 후 서명 환경 부족으로 배포 단계에서 멈췄다.

## 2026-08-02 자동 파 샷 연동 최신 회귀

| 항목 | 결과 |
|---|---|
| 분석 정책 | 성공군 최소 샷에 성공률 5% 이상은 +1회, 미만은 +2회를 더하고 1~5회로 제한 |
| 분석 결과 | 1단계 2회, 2단계 2회, 3단계 3회 |
| 제품 연결 | `LevelDefinition.parShots`, 지도 추천 파, 결과 팝업 별점 기준을 분석 결과와 동기화 |
| 회귀 | 분석 기반 파 정책 경계 테스트·레벨 값 동기화·결과 팝업 Golden 포함 |
| 전체 회귀 | `flutter test` 154개 통과 |
| 정적 분석 | `flutter analyze` 통과, 이슈 없음 |
| Web release | 기존 PID 27962 종료 후 최신 PID 52054로 교체, 루트·`main.dart.js` HTTP 200 |
| Chromium 콘솔 | 390×844·768×1024 모두 오류 0건 |
| 프레임 시간 | idle·발사 평균 16.665~16.667ms, p95 최대 16.8ms, 20ms 초과 0% |
| 캡처 | `390x844-stage-route-par-release.png`, `768x1024-stage-route-par-release.png` |
| iOS | Xcode 컴파일 완료, Development Team·Provisioning Profile 부족으로 배포 단계 실패 |

분석 기반 파와 전략 지표는 결정론적 시뮬레이션 휴리스틱이며 실제 사용자 평균이나 실기기 조작 난이도는 아니다. 실제 30초·5분 사용자 검증은 남은 위험으로 유지한다.

## 2026-08-02 자동 분석 지표 보강 회귀

| 항목 | 결과 |
|---|---|
| 전략 메타데이터 | 각 레벨의 의도 전략과 허용 대체 전략을 선언 |
| 지배 전략 | 1단계 무거움 이전 100.00%(대체 1), 2단계 탄성 이전 100.00%(대체 1), 3단계 무거움 이전 100.00%(대체 0), 고유 성공 입력 합집합 기준 |
| 우연 경로 후보 | 허용 전략 목록 밖 성공 입력 0개 |
| 입력 정밀도 민감도 | 1단계 37.76%, 2단계 43.57%, 3단계 36.52% (각도 10도·힘 5% 격자, 중복 제거 고유 성공 셀 기준) |
| 회귀 | 분석 지표·허용 전략·지배 전략·다중 샷 통합·후속 복제 판정 회귀 포함 |
| 전체 회귀 | `flutter test` 157개 통과 |
| 정적 분석 | `flutter analyze` 통과, 이슈 없음 |

우연 경로 후보는 대체 풀이를 금지하는 성공 판정이 아니라, 레벨 메타데이터에 없는 성공 분지를 찾기 위한 QA 신호다. 입력 정밀도 민감도는 성공 셀의 인접 각도·힘 셀 실패 비율이며, 격자 기반 지표다.

## 2026-08-02 자동 분석 지표 포함 최신 Web release

| 항목 | 결과 |
|---|---|
| 서버 교체 | 기존 PID 52054 종료 후 최신 Web release PID 80147 시작 |
| HTTP | 루트·`main.dart.js` HTTP 200 |
| Chromium 실행 | 390×844·768×1024 콘솔 오류 0건 |
| 프레임 시간 | idle·발사 평균 16.665~16.666ms, p95 최대 16.8ms, 20ms 초과 0% |
| 캡처 | `390x844-stage-route-analysis-release.png`, `768x1024-stage-route-analysis-release.png` |
| 전체 회귀 | `flutter test` 156개 통과 |
| 정적 분석 | `flutter analyze` 통과 |

화면은 분석기만 변경된 반복이라 기존 항해 지도와 시각적으로 동일하며, 캡처는 최신 번들에서 실제 지도 이동을 확인한 증거다. Web 수치는 실제 iPhone/iPad GPU·메모리·터치·햅틱 결과가 아니다.

## 2026-08-02 대체 전략 해석 포함 최종 Web 확인

| 항목 | 결과 |
|---|---|
| 서버 교체 | 기존 PID 80147 종료 후 최신 Web release PID 2452 시작 |
| HTTP | 루트·`main.dart.js` HTTP 200 |
| Chromium 콘솔 | 390×844·768×1024 오류 0건 |
| 프레임 시간 | idle·발사 평균 16.665~16.667ms, p95 최대 16.8ms, 20ms 초과 0% |
| 캡처 | `390x844-stage-route-analysis-final.png`, `768x1024-stage-route-analysis-final.png` |
| 전체 회귀 | `flutter test` 156개 통과 |
| 정적 분석 | `flutter analyze` 통과 |

지배 전략 비중은 고유 성공 입력 공간의 커버리지이며, 대체 전략 수와 함께 해석한다. 이 반복은 화면 레이아웃을 변경하지 않았으므로 캡처는 지도·추천 파·한글 UI가 최신 번들에서 유지되는지 확인한다.

## 2026-08-02 최종 통합 분석·Web 확인

| 항목 | 결과 |
|---|---|
| 자동 분석 | 단일·다중 샷 분석을 `DifficultyMetrics.multiShotMetrics` 통합 경로로 실행 |
| 고유 입력 정밀도 민감도 | 1단계 37.76%, 2단계 43.57%, 3단계 36.52% |
| 다중 샷 복사 없는 성공 | 전체 행동 순서에서 복제 사용 여부를 검사하며 1·2·3단계 모두 존재 |
| 전체 회귀 | `flutter test` 157개 통과 |
| 정적 분석 | `flutter analyze` 통과, 이슈 없음 |
| 포맷 | `dart format` 7개 대상, 변경 없음 |
| 서버 교체 | 기존 PID 90106 종료 후 최신 Web release PID 16872 시작 |
| HTTP | 루트·`main.dart.js` HTTP 200 |
| Chromium 콘솔 | 390×844·768×1024 오류 0건 |
| 프레임 시간 | 390×844 idle·발사 평균 16.666ms, 768×1024 idle 16.667ms·발사 16.665ms, p95 최대 16.7ms, 20ms 초과 0% |
| 캡처 | `390x844-stage-route-analysis-final.png`, `768x1024-stage-route-analysis-final.png`, 현재 상태 `*-home-current.png`·`*-map-current.png`·`*-play-current.png` |
| iOS | Xcode 컴파일 완료, Development Team·Provisioning Profile 부족으로 배포 단계 실패 |

최신 분석 리포트는 1단계 73개, 2단계 72개, 3단계 59개의 고유 성공 입력을 기록했다. 1·2단계의 무속성 풀이와 3단계 고해상도 무속성 우회 풀이는 허용된 대체 경로로 유지된다. Web·분석 수치는 실제 iPhone/iPad 터치·GPU·메모리·햅틱이나 사용자 표본을 대체하지 않는다.

현재 PID 16872에서 캡처 도구를 좌표 기반으로 재실행해 390×844·768×1024의 홈·섬 지도·플레이 상태를 각각 생성했다. 세 상태 모두 Chromium 콘솔 오류는 0건이며, 텍스트 locator가 아닌 Flutter Web 좌표를 사용한 보조 증거라는 한계를 기록한다. 결과 팝업 Golden까지 포함해 전체 Flutter 테스트는 159개다.

## 2026-08-02 충돌 이벤트 순서·튜토리얼 안내 보강

| 항목 | 결과 |
|---|---|
| 물리 애니메이션 이벤트 | 충돌·이동 콜백을 경로 인덱스 순으로 통합하고 같은 시점에는 충돌을 먼저 방출 |
| 지연 프레임 보조 | 첫 Flame 프레임이 늦어도 확정 이벤트를 한 번씩 방출한 뒤 완료 처리 |
| 회귀 | 30·60·120Hz 중복 방출, 동일 경로 동률 순서, 배경 복귀, 마지막 경로점 렌더 테스트 |
| 튜토리얼 UX | 첫 속성 선택 전 상단 상태·하단 반복 문구를 접고 보드 코치마크·목표를 우선 표시 |
| Golden | `game_screen_stage{1,2,3}_{390x844,768x1024}.png` 갱신 후 통과 |
| 전체 회귀 | `flutter test` 157개 통과 |
| 정적 분석 | `flutter analyze` 통과, 이슈 없음 |
| 최신 Web | 기존 PID 90106 종료 후 PID 16872로 교체, 루트·번들 HTTP 200 |
| Web 성능 | 390×844·768×1024 콘솔 오류 0건, 평균 16.666~16.667ms, p95 최대 16.8ms, 20ms 초과 0% |
| iOS 시뮬레이터 | Xcode 컴파일 후 iOS 18.2 Simulator Runtime 미설치로 대상 선택 실패 |

충돌 순서와 튜토리얼 시각 위계는 자동 회귀와 Chromium 화면으로 확인했지만, 실제 iOS 프레임 지연·햅틱과 초보자 이해도는 아직 검증하지 않았다.

## 2026-08-02 결과 팝업 Golden 보강

| 항목 | 결과 |
|---|---|
| 결과 상태 | 최신 레벨 1의 `파 2회 · 3/3 별`, 리더보드, 다음 단계 버튼을 성공 상태로 고정 |
| 화면 기준 | 390×844·768×1024 결과 Golden과 현재 결과 캡처 생성 |
| 한글 렌더링 | NanumGothic 정규·굵은·ExtraBold를 테스트에서 명시 로드해 네모 글리프 방지 |
| 회귀 | 결과 팝업 Golden 2개와 파 표시 위젯 검증 통과 |
| 전체 회귀 | `flutter test` 159개 통과 |
| 정적 분석 | `flutter analyze` 통과, 이슈 없음 |
| 서버 | PID 16872 유지, 앱 코드 변경 없이 결과 테스트·증거만 갱신 |

결과 Golden은 테스트 호스트의 고정 글꼴 기준이며, 실제 iPhone/iPad의 글꼴 rasterizer와 화면 색은 실기기 검증 대상이다.
