# 검증 결과

실행 일시: 2026-08-01 KST

| 명령 | 결과 |
|---|---|
| `dart format --output=none --set-exit-if-changed .` | 통과, 변경 없음 |
| `flutter analyze` | 통과, 이슈 없음 |
| `flutter test` | 통과, 93개 |
| `flutter build web --release` | 통과 |
| `curl http://127.0.0.1:8080` | HTTP 200, 새 빌드 서버 |
| `flutter build ios --no-codesign` | Xcode 빌드 완료 후 Development Team/프로비저닝 프로파일 부족으로 배포 빌드 실패 |

기존 8080 프로세스를 종료한 뒤 새 빌드 서버를 시작했고 HTTP 200 응답을 확인했다.

## 상용 수직 슬라이스 반복 추가 검증

- 보드 바깥 배경·목재 프레임·잔디 필드의 시각 계층을 추가했다.
- 조준 화살표와 파워 게이지의 색상·그림자·끝점 표시를 분리했다.
- 저파워·고파워 실패 원인 이벤트를 추가했다.
- 활성 공의 직접 충돌과 홀 포획을 `ShotImpact`로 기록했다.
- 충돌 위치·법선·대상·경로 시점 단위 테스트를 추가했다.
- 전체 테스트 93개 통과와 `flutter analyze` 통과를 확인했다.
- 실제 렌더링 골든 테스트, iOS 실기기, iPhone/iPad 프레임 측정은 아직 미검증이다.

이번 검증에는 벽 네 방향 경계, 연쇄 벽 반사, 돌·상자·젤리 혼합 재질 연쇄 순서, 최신 위치 전달, 정확한 벽 모서리 대각선 법선, 벽 충돌 법선 시각 피드백, 상태가 비고체여도 벽을 물리 장애물로 처리하는 불변식, 미리보기와 실제 벽 후보 일치, 비정사각형 원형 히트박스, 반발값에 따른 상자 이동량 차이, 2단계 직선 우회 차단, 3단계 점착 발판 선행과 진행 안내, 연쇄 스위치 점착 조건, 탄성 학습 단계의 일반 공 감속, 동일 질량 공 충돌, 점착 지속성, 홀 경계 성공·실패, 실패 복구 패널, 팝업 접근성 격리, 접근성 힘 조절, 320px 안내 말줄임 방지, 발사 중 정보 팝업 차단, 보드 우선 반응형 레이아웃, 앱 생명주기 충전 취소, 조기 롱프레스 취소, 안전영역 팝업 경계, HUD·컨트롤 비겹침, 휴대폰별 한글 접근성 의미, 기본 플랫폼 사운드·햅틱 연결, 각도·힘 격자 기반 단계별 의도 풀이 존재와 무속성 우회 부재, 홀 속성·상자 선행 계약, 과거 공 계약 우회 방지, 고정 상자 이동 이벤트, 점착 재질별 첫 충돌, 상단 보드 배치와 첫 샷 안내 회귀가 포함되며 전체 88개 테스트가 통과했다.
