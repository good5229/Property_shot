# Harness Runs

This directory is for runtime-level run records created by wrapper tools such as
`harness-run`.

## Purpose

- preserve lightweight session metadata
- record which provider was used
- keep task, issue, and branch linkage near the local harness docs

## Notes

- Keep these records concise.
- If run logs become noisy, summarize or prune them with local project rules.
