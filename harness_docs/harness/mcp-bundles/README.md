# MCP Bundles

This directory is for optional repo-local MCP bundle notes.

Use it only if this project wants to define reusable MCP sets above the
operating harness.

## Notes

- This is optional.
- Keep MCP decisions separate from `AGENTS.md` and `docs/rules/`.
- Do not store secrets here.
